{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "chrome",
      "request": "launch",
      "name": "Open in Chrome",
      "url": "http://localhost:8080/dev",
      "webRoot": "${workspaceFolder}",
      "pathMapping": {
        "/demo/": "${workspaceFolder}/dev/"
      }
    },
    {
      "type": "browser-preview",
      "request": "launch",
      "name": "Browser Preview",
      "url": "http://localhost:8080/dev/",
      "webRoot": "${workspaceFolder}",
      "pathMapping": {
        "/demo/": "${workspaceFolder}/dev/"
      }
    }
  ]
}
