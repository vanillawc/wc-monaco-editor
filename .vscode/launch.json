{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "chrome",
      "request": "launch",
      "name": "Chrome",
      "url": "http://localhost:5500/dev",
      "webRoot": "${workspaceFolder}",
      "pathMapping": {
        "/dev/": "${workspaceFolder}/dev/"
      }
    },
    {
      "type": "browser-preview",
      "request": "launch",
      "name": "Browser Preview",
      "url": "http://localhost:5500/dev/",
      "webRoot": "${workspaceFolder}",
      "pathMapping": {
        "/dev/": "${workspaceFolder}/dev/"
      }
    }
  ]
}